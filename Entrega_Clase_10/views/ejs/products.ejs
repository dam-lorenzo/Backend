<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Productos</title>

    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>
<body>
    <header>
        <nav class="navbar sticky-top navbar-dark bg-primary">
            <h1>EJS</h1>
            <div class="container-fluid">
              <a class="navbar-brand" href="/ejs/product">Cargar Producto</a>
            </div>
        </nav>
    </header>
    <div class="container mt-3">
      <div class="jumbotron">
        <h2 style="color: crimson">Productos en stock</h2>
        <% if(items.length !== 0) { %>
            <div class="table-responsive">
                <table class="table table-dark" style="text-align: center;">
                    <tr style="color: yellow;"> <th>Item</th> <th>Precio</th> </tr>
                    <% for (const index in items) { %>
                        <tr>
                            <td> <%= items[index].name %> </td>
                            <td> <%= items[index].price %> </td>
                        </tr>
                    <% } %>
                </table>
            </div>
        <% } else { %>
            <h3 style="color: black">no existen datos</h3>
        <% } %>
            
      </div>
    </div>
</body>
</html>